# Last updated 2023-07-18
#
# install with
# conda env update -f compile.yml --prune
#
name: compile
channels:
  - defaults
  - conda-forge
dependencies:
  - zlib
  - boost=1.60
  - cmake>=2.8.0
  - python=3.5
  - libxml2>=2.7.8
  - libxslt>=1.1.26
